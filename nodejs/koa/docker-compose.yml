version: '3.8'

services:
  app:
    build: .
    image: nodejs/koa
    ports:
      - "4001:3000"
    environment:
      - APPSIGNAL_APP_NAME=nodejs-koa
    env_file:
      - ../../appsignal.env
      - ../../appsignal_key.env
    volumes:
      - ./app:/app
      - ../integration:/integration
  tests:
    build: ../../support/server-tests
    image: server-tests
    profiles:
      - tests
    depends_on:
      - app
    environment:
      - APP_NAME=nodejs/koa
      - APP_URL=http://app:3000
