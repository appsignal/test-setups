[[paths_to_watch]]
path = "/app"
ignore = [
  "_build",
  "deps"
]

[processes.app]
command = "elixir"
args = ["--sname", "server", "--cookie", "server", "-S", "mix", "run", "--no-halt"]
working_dir = "/app"
