include:
  - path: docker-compose.shared.yml

services:
  app:
    environment:
      - APPSIGNAL_APP_NAME=rails6-shoryuken
  downstream:
    environment:
      - APPSIGNAL_APP_NAME=rails6-shoryuken-downstream
  worker:
    environment:
      - APPSIGNAL_APP_NAME=rails6-shoryuken-worker
