include:
  - path: docker-compose.shared.yml

services:
  app:
    environment:
      - APPSIGNAL_APP_NAME=ruby/rails8-delayed-job
  worker:
    environment:
      - APPSIGNAL_APP_NAME=ruby/rails8-delayed-job-worker
