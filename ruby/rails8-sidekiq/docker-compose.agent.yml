include:
  - path: docker-compose.shared.yml

services:
  app:
    environment:
      - APPSIGNAL_APP_NAME=ruby/rails8-sidekiq
  worker:
    environment:
      - APPSIGNAL_APP_NAME=ruby/rails8-sidekiq-worker
